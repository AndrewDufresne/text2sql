[pytest]
testpaths = tests services packages
markers =
    walking_skeleton: end-to-end test requiring full Phase 1 stack
    phase2: end-to-end test requiring Phase 2 capability stack
    phase3: end-to-end test requiring Phase 3 HITL/Eval capability
    e2e: any test requiring live services
asyncio_mode = auto
